server:
  port: 9090
  tomcat:
    uri-encoding: UTF-8
    accesslog:
      enabled: true
spring:
#  session:
#    store-type: redis
  profiles:
    #    active: prod
    active: dev

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cn.sunguolei.qa.springboot.template.entity
  configuration:
    map-underscore-to-camel-case: true